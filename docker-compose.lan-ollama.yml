services:
  # Optional LAN exposure for Ollama.
  # Internal Docker access via http://ollama:11434 remains unchanged.
  ollama:
    ports:
      - "${OLLAMA_BIND_IP:-127.0.0.1}:${OLLAMA_PORT:-11434}:11434"
